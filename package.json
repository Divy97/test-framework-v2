{
  "name": "test-framework-v2",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc --noEmit -p web/tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "images": "docker build -t test-framework-v2-sandbox:latest . && docker build -t test-framework-v2-agent:latest -f Dockerfile.agent .",
    "db:schema": "docker compose exec -T postgres psql -U postgres -d events -f - < db/schema.sql",
    "db:apply": "tsx --env-file=.env src/cli.ts schema",
    "seed": "tsx --env-file=.env src/cli.ts seed",
    "replay": "tsx --env-file=.env src/cli.ts replay",
    "status": "tsx --env-file=.env src/status.ts",
    "serve": "tsx src/serve.ts",
    "rebuild": "tsx --env-file=.env src/cli.ts rebuild",
    "plane": "tsx src/plane-server.ts",
    "runner": "tsx src/runner-main.ts",
    "images:push": "sh scripts/push-images.sh",
    "worker:pair": "tsx --env-file=.env scripts/pair-worker.mts",
    "worker:unpair": "tsx --env-file=.env scripts/unpair-worker.mts",
    "web:build": "npm --prefix web ci && tsc --noEmit -p web/tsconfig.json && npm --prefix web run build",
    "web:dev": "npm --prefix web run dev",
    "test:full": "npm run web:build && vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@vercel/sandbox": "^3.2.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "@types/react": "^19.1.9",
    "@types/react-dom": "^19.1.7"
  }
}
